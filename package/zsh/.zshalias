# aliases
alias zsource='source ~/.zshrc'

alias ll="ls -Flh"
alias la="ll -A"

alias mv="mv -f"
alias cp="cp -f"
alias rm="rm -f"

alias g='git'
alias vim='nvim'

# terraform
alias t='terraform'

# kubectl
alias k='kubectl'
alias mk='minikube'

# reload configuration
alias sz='source ~/.zshrc'
